# Server Configuration
PORT=8090
NODE_ENV=development
BRANCH=dev  # Options: dev (uses test keys) or main (uses live keys)

# Frontend URL (for CORS, redirects, etc.)
FRONTEND_URL=http://localhost:5173  # Development
# FRONTEND_URL=https://tipu-two.vercel.app  # Production

# Firebase Admin SDK
# Get these from Firebase Console → Project Settings → Service Accounts
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@your-project.iam.gserviceaccount.com

# Stripe - TEST KEYS (development/dev branch)
# Get these from Stripe Dashboard → Developers → API Keys (Test mode)
STRIPE_TEST_SECRET_KEY=sk_test_...
STRIPE_TEST_WEBHOOK_SECRET=whsec_test_...

# Stripe - LIVE KEYS (production/main branch)
# NEVER commit these! Add to .env.production or environment secrets only
# Get these from Stripe Dashboard → Developers → API Keys (Live mode)
STRIPE_LIVE_SECRET_KEY=sk_live_...
STRIPE_LIVE_WEBHOOK_SECRET=whsec_...

# Email Service (SendGrid)
SENDGRID_API_KEY=SG.your_api_key_here
EMAIL_FROM=noreply@tipu.com
EMAIL_MOCK_MODE=true

# CORS - Allowed Origins (comma-separated list)
# Include all frontend URLs that should be allowed to access the API
# Development: Multiple localhost ports for different dev servers
# Production: Your deployed frontend URL(s)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080,http://localhost:8081,https://your-app.lovableproject.com

# App URL
APP_URL=http://localhost:8090

# Cron Jobs
CRON_SECRET=your-cron-secret-here

# Microsoft Teams Integration
# Azure AD App Registration credentials
# Get these from Azure Portal → App Registrations → Your App
MICROSOFT_TENANT_ID=your-tenant-id-here
MICROSOFT_CLIENT_ID=your-client-id-here
MICROSOFT_CLIENT_SECRET=your-client-secret-here

# Teams Meeting Settings
# REQUIRED: Object ID (GUID) of the service account that will create all meetings
# Get from: Azure Portal → Microsoft Entra ID → Users → [Your User] → Object ID
# Must be a valid GUID format: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
TEAMS_ORGANIZER_USER_ID=your-organizer-object-id-here

# OPTIONAL: Email of the service account (used for logging and debugging)
# Must be a Microsoft 365 account in your tenant with Teams license
TEAMS_ORGANIZER_EMAIL=your-organizer-email@yourdomain.com
